# Production web config
# Copy to .env.production for local production builds

VITE_API_BASE_URL=https://your-backend-domain/api

# Optional demo/admin fallback values (avoid in production if possible)
# VITE_DEMO_PHONE=
# VITE_DEMO_PASSWORD=
# VITE_ADMIN_PHONE=
# VITE_ADMIN_PASSWORD=
